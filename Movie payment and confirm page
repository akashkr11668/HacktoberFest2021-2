
<!DOCTYPE html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="Generator" content="EditPlus®">
  <meta name="Author" content="">
  <meta name="Keywords" content="">
  <meta name="Description" content="">
  <title>Booking</title>
  <style>
  html{
  height: 100%;
}

body{
  font-family: "Arial", Helvetica, sans-serif;
  position: relative;
}

#container{
  text-align: center;
  position: relative;
  height: 100%;
}

#movies_booked, #form{
  display: inline-block;
  width: 40%;
  height: 100%;
  margin: 0 auto;
  vertical-align:text-top;
}

fieldset, {
  border:1px solid #AED0EA;
  border-radius:8px;
  box-shadow:0 0 10px #D7EBF9;
}

legend, {
  color: #2779AA;
  font-size: 120%;
  text-align: center;
  background-color: white;
}

p{
  overflow: hidden;
}

label{
  width: 50%;
  text-align: right;
  float: left;
  clear: both;
  color: #2779AA;
}

p input, p select{
  width: 40%;
  -moz-box-sizing: border-box;
  box-sizing: border-box;
  float: left;
  margin-left: 5%;
  margin-right: 5%;
  color: #2779AA;
}

{
  color: #2779AA;
}

p span, li{
  text-align: left;
}

{
  text-align: center;
}

{
  font-size: 13px;
}

{
  text-align: left;
}

{
  font-size: 9px;
}
  </style>
 </head>
 <body>
  <div id="container">
  <div id="form">
    <form id="book_tickets" >
      <fieldset>
        <legend>Movie Booking Details</legend>

        <p>
          <label for="">Movie</label>
          <select id="" name="" required autofocus>
            <option></option>
            <option value="Radhey">Radhey </option>
            <option value="Justice League ">Justice League </option>
            <option value="Kong vs Godzilla">Kong vs Godzilla</option>
    <option value="Parasite ">Parasite </option>
     <option value="Joker ">Joker </option>
          </select>
        </p>

        <p>
          <label for="">Theaters</label>
          <select id="" required>
            <option></option>
            <option value="Cinepolis">Cinepolis</option>
            <option value="New Plaza Cinema">New Plaza Cinema</option>
            <option value="Mohini Cinemas">Mohini Cinemas</option>
          </select>
        </p>

        <p>
          <label for="">Date<br/></label>
          <input type="date" name="" id="" min="today" required />
        </p>

        <p>
          <label for="">Email</label>
          <input type="email" name="" id="" required />
        </p>

        <p>
          <label for=""># Tickets</label>
          <input type="number" min="1" name="" id="" required />
        </p>

         <p>
          <label for="">Tickets Price</label>
          <input type="number" id="" required />
        </p>
        <div id="">
          <input type="submit" id="submit"  value="Book Tickets" />
        </div>
      </fieldset>
    </form>
    
</div>

     

</div>

 </body>
</html>



<p>
  <label>Total Price</label>
  <span id="total_price">(enter data first)</span>
</p>

</fieldset>
</form>
<p id="perTicketPrice">Per ticket price = &#x20b9; 100.00</p>
<p id="discount">
<span id="disount_title">Discounts:</span>
<ul>
<li>7% discount if show is on weekday</li>
<li>20% discount if number of booked shows >= 20</li>
</ul>
</p>
</div>

<fieldset id="movies_booked">
<legend>Movies Booked Till Date</legend>
<span id="movies_list"></span>
</fieldset>		    	

</div>


<script id="worker" type="javascript/worker">
self.onmessage = function msgWorkerHandler(event){
    var jsonString = event.data;

    var day = jsonString.day;
    var number_booked_shows = jsonString.number_booked_shows;
    var tickets_quantity = jsonString.tickets_quantity;

    // set price of each ticket as Rs. 100
    var totalPrice = tickets_quantity * 100;

    // 7% discount if on weekday
    if(day > 0 && day < 6){
        totalPrice = totalPrice - 0.10 * totalPrice;
    }

    // 20% discount if number of booked shows >= 10
    if(number_booked_shows >= 10){
        totalPrice = totalPrice - 0.10 * totalPrice;
    }

       postMessage("&#x20b9; " + totalPrice);
}
</script>
